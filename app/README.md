# Lancer Application

Future web application for Lancer TTRPG.

This directory is a placeholder for the application layer that will consume the type-driven schemas from `/core/`.

## Planned Features

- Character builder using `core.pilot` schemas
- Mech loadout manager using `core.mech` schemas (future)
- Combat tracker using `core.combat` schemas (future)
- Campaign management tools

## Tech Stack (TBD)

- FastAPI or similar for backend
- Database using SQLModel (Pydantic + SQLAlchemy)
- Frontend framework TBD

